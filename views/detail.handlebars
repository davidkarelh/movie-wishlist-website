<nav class="navbar navbar-expand-lg navbar-light bg-dark">
  <div class="container-fluid">
    <a class="navbar-brand text-light" href="/">Hi, {{user.username}}!</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <a class="nav-link active text-light" aria-current="page" href="/">Home</a>
        </li>
        <li class="nav-item">
          <a class="nav-link text-danger" href="/logout" >Log Out</a>
        </li>
      </ul>
      <form class="d-flex">
        <a href="/wishlist" class="btn btn-outline-success" type="submit">My Wishlist</a>
      </form>
    </div>
  </div>
</nav>

<div class="card mx-auto mt-2 mb-5 bg-dark" style="width: 30rem;">
    <img src="http://image.tmdb.org/t/p/w500{{detail.poster_path}}" class="card-img-top" alt="...">
    <div class="card-body">
        <h5 class="card-title text-light">{{detail.original_title}}</h5>
        <p class="card-text text-light">{{detail.overview}}</p>
        <p class="card-text text-light">{{detail.release_date}}</p>
        {{!-- {{#if notWishlist}}
          <button  type="button" class="btn btn-success" onclick='clickHandler(this.id)' id="{{detail.id}}">Add to Wishlist</button>
        {{else}}
          <button  type="button" class="btn btn-dark disabled" onclick='clickHandler(this.id)' id="{{detail.id}}" disabled>In Your Wishlist</button>
        {{/if}} --}}
        {{!-- <a href="#" class="btn btn-success">Add to Wishlist</a> --}}
    </div>
</div>
